# Mono sync — 2026-08-28

Reconciliation of this repo with `nightheron-mono` in **both** directions, joining the positive
changes from each. Previous sync: [2026-08-17](mono-sync-2026-08-17.md) plus its addenda to 08-22.

> **Scope of this document.** It covers **Phase A** (mono → demo forward-port), written
> incrementally as each group lands rather than all at once, so nothing is lost if the work pauses.
> **Phase B** (demo → mono and demo → framework backports) is deliberately **held** until Phase A is
> verified; its evidence is recorded at the bottom so it survives the gap.
>
> **Phase A complete: Groups 5a, 1, 2, ADR-0031, 3, 4, 5b, 6 all landed** (this section header
> was updated as each landed). ADR-0031 (Group 2 item 11) was split into its own PR — a ~48-file
> replay-affecting identity change does not belong on a UI-correctness diff.

Evidence base: the 2026-08-28 divergence-audit run (full §2.4 coverage, 0 unresolved, 25 pairs over
expectation), the demo sync ledgers, and code-level who-is-ahead verdicts per flagged pair.

## Group 1 — framework re-vendor

Source of truth is the sibling `nightheron-state-machine` repo (ADR-0012), **not** the mono's
`packages/`. Framework `main` @ `e8cd979`.

### Ported

| What                                                                                                                                                                                                                                                                                                                    | Source                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| **Issue #11 cancellation fix**, as one 3-file unit: the driver's cancellation arm now records a `__terminal:cancelled` row with `trigger.kind: 'cancellation'`, then `recorder.close()` + `flushNow()`; `flushNow()` added to `transition-sink.ts`; `'cancellation'` added to `TransitionTrigger['kind']` in `types.ts` | `d0518d8` (framework PR #12) |
| **`machine-validation.ts`** (+ its tests), new file: the waiting-state-timeout startup check extracted as pure, testable functions, raising `StateMachineDeclarationError` and naming both the machine and the offending states                                                                                         | `8e41525` (framework PR #14) |
| **`shouldContinueAsNew`**: the SDK's `continueAsNewSuggested` leads; the input-count threshold is a ceiling only. Drops the old `\|\| 100`, which coerced an explicit threshold of `0` into `100`                                                                                                                       | `8e41525`                    |
| **`flushNow()` tests** in `transition-sink.test.ts` — purely additive, and deliberately never start the coroutine, since a test that called `runFlusher()` first would exercise the healthy path and pass against the bug                                                                                               | `d0518d8`                    |

### Why the cancellation fix matters here

The timeline used to stop at the last ordinary transition, with no terminal row and no gap marker,
so a cancelled workflow read as **still sitting in its last state**. Two independent causes, both
fixed: nothing _recorded_ an ending (the only `record()` call site is inside the main loop, and
external cancellation interrupts the loop rather than transitioning through it), and the flush
could not _survive_ (`shutdownRecorder()` awaits a coroutine created in the workflow's root scope,
already rejected by then; `CancellationScope.nonCancellable` only protects operations started
inside it).

### Reconciled — kept this repo's version, deliberately

Vendoring wholesale would have imported regressions. Each of these is a case where **the demo is
ahead of the framework**:

| Kept                                                           | Why                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`UNRESOLVED_TIMEOUT_FALLBACK` park-and-warn** in `driver.ts` | The framework replaced it with `stateConfig.timeout!`, justified by "the startup check guarantees a declaration exists". That is unsound: the startup check explicitly counts a `timeout` **function** as declared, and a function can resolve `undefined` on any given iteration. The assertion is wrong precisely where the fallback exists. Backported to the framework in Phase B |
| **`driver.test.ts` in full**                                   | This repo's file has **24 tests to the framework's 18**. The framework's lacks the park-and-warn test, both continue-as-new trigger tests, and two startup-check tests, and would have deleted the `warnCalls` capture that makes the park-and-warn assertable                                                                                                                        |
| **The `continueAsNewThreshold` doc comment** in `types.ts`     | The framework deleted it outright. It is correct as written — it already describes the threshold as secondary to the SDK suggestion — so only the _code_ needed the fix, not the documentation                                                                                                                                                                                        |
| **`machine.ts`** (not ported)                                  | Comment-only difference; this repo's cites its own backlog #18. Cosmetic                                                                                                                                                                                                                                                                                                              |
| **`identity.ts`** (deferred, not skipped)                      | The framework's wording is correct **under ADR-0031**, which this repo has not adopted yet — it lands in Group 2. Taking the comment now would make it describe behaviour the code does not have. Deferred so comment and code land together                                                                                                                                          |

### Added here, because it exists in neither repo

A test that cancellation **records** a terminal row and flushes it without the coroutine. The
framework's issue-#11 fix shipped untested; this repo's suite had no coverage either. Fail-proved
by removing the recording and confirming that test — and only that test — fails.

### Replay compatibility

Driver changes affect **parked** workflows: precedent `be1ea07`, where old-code children hit
`TMPRL1100` on replay. Posture here is unchanged — **new executions only**, and restart workers
after building (the stale-worker rule).

### The prettier dividend

`nightheron-state-machine` had no prettier config, so its source formatted to defaults (double
quotes, ~80 col) while both consumers enforce `singleQuote` / `printWidth: 100`. Every re-vendor
therefore needed a reformat pass, and the audit re-flagged those rows forever.

Fixed at the source in framework PR #15 before this group started. The measurable result: **14 of
21 shared framework files are now byte-identical** to the vendored copies, and every file copied
verbatim in this group passed `format:check` here **with no reformatting**. Future re-vendors of
untouched files are a plain `cp`.

### Open question for a later pass

Vendored framework files diverge in doc-comment **references**: the framework cites
`mono-backlog-068/069`, this repo cites its own `#20`/`#22`. Adapting them on each vendor is what
makes the files diverge permanently — the same trap the prettier fix just removed. New files in
this group were copied **verbatim**; existing files kept their adapted references, via surgical
edits rather than whole-file replacement. Worth deciding deliberately: converge on verbatim
vendoring, or keep adapting and teach `audit.mjs` to ignore comment-only deltas.

## Group 2 — contracts + checkout correctness chain

Sources: mono #229/#230 (checkout-owned cart version), #207-for-checkout (derived step on
responses), run-010 Defect #2 (`computeCheckoutTotal`), run-009 (field-keyed address validation).

### Ported

| What                                                           | Detail                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Checkout-owned `cartVersion` + `hasUnacknowledgedCartChange()` | `CheckoutState.cartVersion` is the version THIS checkout priced; the shared predicate compares two checkout-owned numbers. `CartChangedBanner` now uses it — one workflow, one RPC, one authority                                                                                                                                                        |
| `asCheckoutState()`                                            | The ONE projection from context to the wire `CheckoutState`. The query, every update response, error echoes, and `finalState` all go through it; previously the same `step` injection was written four times and none of the four projected `cartVersion`                                                                                                |
| `computeCheckoutTotal()`                                       | Subtotal − discounts + LIVE shipping/tax. The review page's Total row and Place Order button read `cart.totalPrice` — the pre-address estimate — while the Shipping/Tax rows above them read live state: the mono's run-010 shown-$107.96-charged-$116.05 defect shape, live here. Both now use the helper                                               |
| Shipping-address validation suite (~175 lines + tests)         | `REQUIRED_SHIPPING_ADDRESS_FIELDS`, `US_STATE_CODES`, `SUPPORTED_SHIPPING_COUNTRIES`, flat + field-keyed validators. Wired into the shipping page with the mono's touched-on-blur pattern (`FieldError`, `role="alert"`); submit validates all fields before any network work. The form previously relied on HTML `required` plus one manual email check |

### Verified already present — NOT ported (stale who-is-ahead verdicts)

| Plan claim                                                               | Reality                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "Fixes a LIVE demo bug: banner fires on every fresh checkout"            | The demo fixed this itself — `validating` re-syncs `cartVersionAtStart`/`Acknowledged` from the priced cart, and the code comment at `checkout/states.ts` documents exactly this false positive (its backlog #3). The port still removes the _class_ (cross-workflow version race); the instance was gone |
| "Update responses lack the derived step"                                 | `workflows.ts` already injected `step` via `stateFormatters` into every update response and error echo. What was genuinely missing was `cartVersion` on those responses, and the four-fold duplication                                                                                                    |
| "version-neutral SubmitFreezeStarted (744762a6) where the demo lacks it" | Already present: `SubmitFreezeStarted` is classified `false` in `CONTENT_EVENTS` (`cart/states.ts:783`)                                                                                                                                                                                                   |
| mono #273 paid-then-edited fix                                           | ADR-0028/Stripe territory, which this plan skips; the demo's `CART_CHANGED` submit guard (its 3fc8e6d) covers the mock-gateway equivalent                                                                                                                                                                 |

### Deliberately not ported

- **`checkout/page.tsx`'s totals** stay on `cart.totalPrice`: that summary renders _before_ a
  checkout exists, so there is no live state to mix with — both rows share one provenance, which
  is the thing the run-010 defect requires breaking.
- **CONTENT_EVENTS `Record` shape** stays as-is — the demo is ahead of the mono's `Set` here
  (Phase B item, demo → mono).

## Group 3 — storefront UI

### Ported

- **`checkout-routing.ts`** + tests: `pathForStep` / `routeForStep` (with a reason, so a bounce is
  never silent) / `destinationFor`, the fixed-point property over the whole `CheckoutStep` union,
  and the source-scan ratchet banning literal `router.push('/shop/checkout/…')`. This repo had
  **five** literal call sites (four pages + CartDrawer) plus its own inline `routeForStep` copy in
  `checkout/page.tsx` — all now route through the module. The ratchet scans the checkout dir AND
  `components/` (CartDrawer was an offender outside the mono's scan scope), carries a
  found-the-sources control, was tightened to the three step surfaces (`…/confirmation` is
  legitimately composed at call sites — its href carries order params the module refuses to own),
  and was fail-proved with a planted literal.
- **`cart-change-diff.ts`** + 12 tests: this repo's own R3 inline diff logic, taken back in the
  parent's extracted shape. Totals through `computeCheckoutTotal`, never `cart.totalPrice`.
- **CartChangedBanner** restructured to the parent's shape: optional `checkoutState` prop (the
  review page passes its own polled state, so the banner stops double-polling), `dismissedVersion`
  local record, and **refresh once per raised version** instead of `refreshCart()` every 3s tick —
  the parent's run-016 Defect #3 (a stale-cart diff shows a WRONG total) plus load for nothing.
- **CartContext**: `resolved` → `initializing` (name-parity with the parent; semantics inverted at
  every consumer), `.catch()` on BOTH bootstrap promises — `getCartId().then()` had none, so a
  rejected bootstrap pinned the flag and spun forever — and the `step === 'failed'` watcher that
  surfaces a checkout failure for recovery. `addItem`'s `sku` param renamed `variantId` (it never
  was a sku — mono #252's lesson).
- **`review-screen.ts`** + 7 tests, copied **verbatim** (pure, no repo-specific imports): the
  four-state ordering with `placed` FIRST. Wired with a `placedHref` set before `clearCart()` —
  closing the #255 window where the post-payment re-render showed the missing-cart screen.
- **CartDrawer**: the checkout-in-progress advisory (say so BEFORE the shopper edits), the
  checkout-failure recovery banner with dismiss, the generic context-error banner, and the
  **honest total** — Tax row dropped, Total = subtotal − discounts, "Shipping and tax calculated
  at checkout" (both are address-derived and a cart has no address; run-016 Defect #5's shape).
- **cart-actions**: `ok` made TOTAL across every outcome (both throw paths lacked it — mono #350)
  with the ported ratchet test (control + per-kind + no-line-lacks-it); the `step === 'complete'`
  cookie delete routed through `retireCartCookie` so an older tab completing checkout cannot clear
  a NEWER cart's cookie (this site predated c6d278d's guards), pinned by a new two-tab test.
- **Barrel**: `communications` + `activity-tagging` exported, aligning it with the parent's.

### Verified — NOT ported (stale plan verdicts, continuing the series)

- **"retype OrderCommunications off the deep `contracts/elasticsearch` import"** would be a
  regression: the deep imports are deliberate and commented ("Direct module import (not the
  contracts barrel) — pure module, PR #41"). This barrel carries Temporal update/query
  definitions, so client components avoiding it IS the parent's R8/#45 lesson, which the plan
  itself says to carry. A note now guards the convention in the barrel itself.
- **Client checkout gate removal**: no such gate exists here — nothing to remove.

### For Phase B

The parent's own CartDrawer comment still reads "The cartId IS the journey's correlationId
(ADR-0022)" — stale after its own ADR-0031. Comment-only mono fix.

## ADR-0031 — the correlation id is its own key again (split from Group 2)

Sources: mono `a73e16f0` + `3ce5a7f5` (its R11), adapted to this repo's smaller surface — no
`user_carts` (cookie-only identity), no Stripe webhook, no ops sweeps, no Twisp.

### Ported

- **`requireCorrelationId()`** in `contracts/constants.ts` (pure, Temporal-free), and
  `buildWorkflowStartOptions` now **throws on a falsy-but-defined** correlationId instead of
  silently dropping the Search Attribute. `undefined` stays the documented singleton opt-out.
- **The cart workflow owns the journey key**: minted as its own UUID in `executeCartUpdate`
  (cookie-cached, scoped `<cartId>:<correlationId>` so it self-invalidates), returned on
  `CartDetails` from BOTH read paths — the query and every update response, each reading the
  workflow's own `CorrelationId` Search Attribute — and reconciled into the cache after every
  mutation (`USE_EXISTING` discards start options, so the minted value is never trusted).
- **Four entity-key fallbacks replaced with the throwing accessor**: cart→checkout child start,
  fulfillment→fulfiller-order child start, the reservation ES document, and `createOrder` (whose
  "keeps seed/api paths working" comment was stale — the checkout workflow is its only caller).
  `?? order.correlationId` / `?? doc.correlationId` reads stay: they carry the journey key the
  record itself holds, which is a journey-key source, not an entity-key fallback.
- **`framework/identity.ts`** takes the framework's ADR-0031 wording — now **byte-identical**
  with `nightheron-state-machine` (15 of 21 files converged).
- **ADR corpus**: 0031 copied with the Provenance/Divergence banner; 0022 marked superseded in
  part; 0019 restored with its 2026-07-30 entity-key-fallback amendment **withdrawn** (that
  amendment cited "exactly as temporal-commerce-demo does" — this repo was the fallback's origin,
  and it now removes it); README index updated.

### Tests

Three old-model pins updated, each verified to discriminate: the start-options assertion now
requires the CorrelationId to be a UUID **and differ from** the cartId (presence alone passes
under the welded model); the two cookie-retirement tests expect both identity pointers to retire
together; and the states test asserting the exact cartId fallback ADR-0031 removes is inverted to
assert the refusal (`rejected: true`, `/No CorrelationId available/`, no child started). New:
`requireCorrelationId` + empty-string-throw coverage in `constants.test.ts`, whose existing
independence tests passed unchanged — the model is being restored, not invented.

### Replay + posture

Clean break, new executions only: an in-flight journey started under the welded model would hit
the checkout-child-start throw on its next transition. Restart workers after build; a `dev:init`
wipe is the honest reset.

## Group 4 — replay gate, worker versioning, log rotation

### Ported

- **The replay gate** (`src/test-support/replay-registry.ts` + `workflow-replay.test.ts` +
  `scripts/capture-workflow-histories.ts`, `npm run histories:capture`). Redesigned around this
  repo's e2e journey rather than per-domain scenarios: ONE drive — cart → checkout → OMS →
  fulfillment → feedback — captures **three full-lifecycle fixtures** (cart 112 events, checkout
  135, oms 297), every one through its terminal event. Both parent lessons carried: every wait is
  bounded (`pollUntil`/`settle` — a capture must never hang) and the stub sets mirror the e2e's
  exhaustively (a missing stub does not fail a capture; it silently pins a failure path).
  **Fail-proved per domain**: a planted `sleep(1)` in each of the three workflows fails its
  domain's replay with `TMPRL1100`; restored byte-identically.
  One genuine demo difference recorded in the script: `queryCart` here is a LIVE bridge to the
  parent cart workflow (in the parent platform it is a plain stubbable read), so the checkout
  capture genuinely needs the cart worker co-running.
  Found while building it: settling a handle whose workflow does not exist yet reads as an
  immediate failure, and with nothing awaiting a result the env's time-skipping stays LOCKED —
  the first capture run stalled at `shipped` exactly that way. The script now waits for OMS
  intake to start the child first.
- **Worker Versioning opt-in** (`src/lib/worker-versioning.ts`, ADR-0030): `WORKER_BUILD_ID` →
  `PINNED` deployment version, `{}` when unset so the default launch is byte-identical. Spread
  into every domain worker by the launcher alongside the otel config.
- **UTC day-roll for the file log sink** (parent's mono-issue-0320 fix + its rotation tests,
  4 new): a long-running worker kept appending to the day-one file forever, so anything tailing
  today's date read an EMPTY population as a clean report. Improved in one respect: the prune
  cutoff derives from the sink's (injectable) clock rather than an unconditional `Date.now()` —
  with a real-clock cutoff, the rotation test's fixture files age out of retention the moment
  the calendar moves a week past its literal dates.
- `@temporalio/proto` made an explicit devDependency, pinned EXACTLY `1.19.0` — `^1.19.0`
  resolved to 1.23.0 alongside the SDK's nested 1.19.0, and two protobufjs registrations die
  with `duplicate name 'ActivityHeartbeat' in Namespace coresdk`.

### Verified already covered — NOT ported

- **All three inventory items** (plan item 20), by this repo's own commits, as the plan asked to
  check first: bounded CAS retry — `CAS_MAX_ATTEMPTS = 3`, throwing `InventoryContentionError`
  on exhaustion (its #20, `37a22df`); group reservation by sku (`2b23ab6`); in-place renew
  (`9e86402`).
- **`passWithNoTests`** (item 21): absent. The timer-durations-from-history technique has no
  target here — the identity domain simplification (its ADR-0006) stands.

### ADR corpus (item 22)

0027 and 0030 copied with Provenance/Divergence banners (0031 landed with the ADR-0031 PR);
0030's mono-only links absolutized to the parent repo's GitHub paths; README index updated.
0030's divergence note records that this closes backlog #21 the way its "the mono decides
first" note anticipated. ADR-0028 stays skipped (Stripe).

### Found in the PARENT while porting (Phase B, but one is urgent)

- **The mono's own `logger-rotation.test.ts` is failing on its `main` today** — its prune cutoff
  is real-clock, so the test's literal dates (2026-08-21/22) aged out of the 7-day retention when
  the calendar reached 2026-08-29 UTC. Verified by running it: 1 failed / 161 passed. The demo's
  clock-derived cutoff above is the fix to backport. Urgent because every mono PR touching
  `packages/infrastructure` is now red.
- The ledger annotation the plan asked for: this repo's `4dd6178` integer-cents work converges
  with the parent's Money allocator — equivalent by different types; no code port in either
  direction.

## Group 5b — lint + AI-config reconciliation

### ESLint (item 25)

Diffed this repo's `eslint.config.mjs` against the mono's `packages/eslint-config/invariants.js`.
**Already present here** (the plan's port list was stale): the gen-1 decider-surface ban (its
mono #203 example), the terminal-literal ban, the workflow-id template ban, and the
wall-clock/randomness bans in states/decider files. Two genuine gaps ported, both verified clean
before adding (pure ratchets) and **fail-proved** with a throwaway file carrying both violations:

- **Cross-domain workflow-import ban** (invariant #1), mapped to this repo's structure: own-domain
  imports are `./workflows` (unmatched); `../*/workflows` and `@/temporal/*/workflows` are banned
  outside tests, which legitimately import workflow functions to drive real journeys.
- **Clock-in-decide/evolve selectors**: the file-scoped bans miss a decider defined outside a
  states/\*-decider file; these key on the function/property NAME, anywhere in domain source.

**Not portable yet**: the mono's bespoke-transition-write ban (the `insertStatusHistoryEntry`
name-shape) — this repo still HAS `insertStatusHistoryEntry` (the mono retired it, its backlog
#28, in favour of the ADR-0010 recorder). Adopting the ban requires porting the retirement first;
recorded as a mono→demo candidate rather than smuggled into a lint PR. Monorepo-mechanical rules
(package-boundary import bans, tenant-guard lint, `@temporalio/client` indirection) skipped per
plan.

### AI config (item 26)

- `sync-private-docs` skill copies verified **identical** (the plan asked specifically).
- **Both `AGENTS.md` and `.agent/rules.md` still taught the R5 cartId-as-correlation model** —
  the demo's own 2026-08-26 rules refresh predates the ADR-0031 port. Rewritten to the own-UUID
  model, including the throws-not-falls-back contract. `inventory_history` journal-key text
  likewise.
- `AGENTS.md`'s literal "514 tests" count removed — a pinned count rots on every PR; the line now
  names what matters (no containers; includes the replay gate).
- `demo-temporal-worker-changes.md` gains the missing half of its own subject: the replay gate —
  when a `TMPRL1100` failure means fix-the-workflow vs regenerate-fixtures vs Worker Versioning.
- The divergence-audit / forward-port / backport workflows stay mono-side by design.

## Group 6 — ADR unification (immutability waived for this pass)

- **The eight remaining mono ADRs copied** with Provenance/Divergence banners: 0005, 0015, 0021
  (applicable — each divergence note states how the demo exercises it) and 0002, 0008, 0013,
  0014, 0028 (**not exercised** — carried for the reasoning, exactly as the corpus already
  carries 0004). The corpora are now **number-aligned at 29 = 29**, and the strict-subset claim
  was verified before copying (no demo-only numbers).
- **Re-convergence (item 28) was verification, not rewriting.** An amendment-marker scan across
  every shared pair found ZERO mono amendments missing from the demo's copies; the large diff
  counts (0003/0006/0009/0010/0011) are deliberate condensed adaptations, each carrying its own
  Provenance note. The ONE genuine staleness: this repo's 0011 ended at its 2026-08-12
  "cartId IS the correlationId" amendment — now closed with a 2026-08-28 amendment recording the
  ADR-0031 reversal, and its Search-Attribute table row corrected.
- **Unified index (item 29)**: BOTH repos' `docs/adr/README.md` now carry the SAME
  corresponding-numbers table — number, title, status, "applies to" (both / platform-carried) —
  generated once and spliced into each, with the 0017/0018 gap stated as shared and deliberate.
  The mono's own index was stale too (no 0031 row; 0019/0022 statuses unmarked) — fixed by the
  same splice; that edit is the one mono-repo commit in Phase A.
- **Cross-references (item 30)**: 24 mono-relative links in the newly copied ADRs absolutized to
  the parent repo's GitHub, plus two PRE-EXISTING broken links from the 08-26 corpus copy (0009's
  theme-plan anchor, 0025's parenthesized storefront path). The demo ADR corpus now has **zero**
  unresolvable links.

## Closing audit (2026-08-29)

Re-ran the divergence audit after Phase A: **20 pairs over expectation, down from the opening
run's 25** — but the composition is the meaningful result, not the count. A sweep that
deliberately ADAPTS rather than copies (asCheckoutState, the cookie trio, the derived failure
surface, the kept park-and-warn) makes semantic diffs against the parent GROW even as the repos
converge in substance, so the flag list was read pair-by-pair:

- **⚑ ledgered in this document** (deliberate, reasons above): cart-actions, checkout/workflows,
  CartContext, contracts/checkout, contracts/identity, contracts/index, framework/driver
  (the kept park-and-warn + threshold comment — the re-vendor's whole point), identity workflows
  - activities (its ADR-0006 simplification stands), projection-completion pair.
- **Design-diverged by the plan's own skip list**: globals.css, ShopNavBar, OrderCommunications,
  contracts/inventory (variant-keyed vs blank-SKU-keyed — different by design).
- **CartDrawer / CartChangedBanner**: this sweep's Group 3 work (banners, honest total,
  extracted diff) — divergence grew BY porting, since the ports were adapted to this repo's
  cents/types. Expected.

**No pair on the list is un-reasoned drift.** Two audit-script quirks noticed for its next
maintenance pass (not fixed here): several rows print twice (CartDrawer, ShopNavBar,
identity/workflows), and the ⚑ ledger-mention scan misses some files this document plainly
discusses (CartDrawer, CartChangedBanner) — likely matching an older ledger file list.
Coverage: §2.4 maps 114 of 115 mappable files (99%), 0 unresolved rows.

## Phase B — held, recorded so the evidence survives

Not executed. Backport candidates found while doing Phase A:

- **Framework regression**: restore the timeout-function-undefined park-and-warn (and its test) in
  `nightheron-state-machine` — see the reconcile table above.
- **Framework has no test** for its own issue-#11 cancellation fix; the one written here is the
  model.
- Attempt-nonce payment idempotency key (`7f9858a`) — the mono still keys **real Stripe** on
  `${cartId}-${totalPrice}`.
- Clear the latched checkout mirror centrally in `evolve` (`440f991`) — the mono re-raises a stale
  failure to the shopper.
- `VERSION_NEUTRAL_EVENTS`: `Set` → exhaustive `Record` shape (`5a042af`/`d4e87d8`).
- Guard every cart-cookie clear on the mono side (`c6d278d` analogue).
- `checkout/workflows.ts`: try/catch + warn on the parent completion signal — the mono's can fail an
  already-successful checkout.
- stderr on every dropped log forward (`77a8fb2` half); `projection-completion` repository tests.
