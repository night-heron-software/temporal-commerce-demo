# Mono sync — 2026-07-25

Forward-port audit of `nightheron-mono` (`main` @ `2c91659`) against this repo, run the day
after the demo→mono backport recorded in mono's `docs/reference/demo-backport-2026-07-25.md`.
Previous sync: [2026-07-16](mono-sync-2026-07-16.md).

Because most of mono's recent fix commits _originated here_ and were backported to mono on
2026-07-25 (`4f382a3`, `466ac4f`, `4480f79`, `b5696ba`, `20fd6f5` are mono's copies of demo
PRs #20/#24/#35, #31, #45, the ALLOW FILTERING ratchet, and #18/#19), the pending
mono→demo surface was small.

## Ported

| Area                                        | Files                                                                                                                                       | Source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Grafana temporal-server dashboard           | `observability/grafana/provisioning/dashboards/json/temporal-server.json`, `observability/grafana/provisioning/datasources/datasources.yml` | mono `c4d00de`. 12-panel rewrite (At a glance / Frontend service / Workers & task queues / Persistence rows, `$namespace` + multi-select `$taskqueue` variables, declarative `uid: prometheus` datasource — the datasources.yml `uid` addition comes with it). Adaptations: namespace `default`, tags `[temporal, demo]`, demo cadence kept (10s refresh / 30m window), and the demo's three SDK-worker panels (poll success, task slots, SDK schedule-to-start + activity failures — no mono equivalent, fed by the `host.docker.internal:9466` scrape mono lacks) merged into the Workers row. |
| `transferToFulfiller` live-status whitelist | `src/temporal/inventory/db/inventory-command-repository.ts`, `.test.ts`                                                                     | mono's guard (`status !== 'TEMPORARY' && status !== 'CONFIRMED'` → warn + return) replaces the demo's terminal-status blacklist, so a status added later fails safe — the same closed-set bug class `4480f79`/demo #45 fixed for trigger markers. The always-true downstream status checks collapsed accordingly.                                                                                                                                                                                                                                                                                |

## Deliberately NOT ported

- **`transferToFulfiller` counter ordering** — _resolved 2026-07-29_: mono adopted the
  demo's rows-first ordering (mono PR #86), so this is no longer a divergence. The demo
  ordering had been kept on purpose for retry safety (a re-run after the batch landed
  reloads the row with the new attribution and no-ops the counter move); that rationale
  now holds in both repos.
- **`0ac643e` fix(accounting) TOCTOU** — mono-only; the demo has no accounting domain.
- Mono feature work (Communications tab variants, `/dev/orders` search) — unchanged from
  the 2026-07-16 exclusion list; the demo's own #44 covers communications.

## Confirmed already in sync (no action)

- Inventory write-path (`4f382a3`), logging (`466ac4f`, `20fd6f5`), OMS trigger markers
  (`4480f79`), ALLOW FILTERING ratchet (`b5696ba`), eslint invariants (`cc86605`),
  correlation scope-isolation test style (`2c91659`) — demo-originated or independently
  equivalent.
- Framework vendored copy vs `nightheron-state-machine` @ `7b1af7b`: byte-equivalent
  modulo prettier wrapping; `projection-completion.ts` identical.
- Prometheus scrape config: demo is **ahead** (also scrapes the SDK worker on `:9466`).

## Mono-side divergences to respect on the next sync

From mono's `docs/reference/demo-backport-2026-07-25.md` §Mono-specific adaptations: the
demo journals `DRIFT_CORRECTION` under `PLATFORM_CORRELATION_ID` (mono cannot — its
journal is store-partitioned), and the reconciler CAS strategies differ on purpose. A
naive diff will read both as drift; neither is.
