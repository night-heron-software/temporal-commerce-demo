# Mono sync — 2026-08-17

Full catch-up forward-port from `nightheron-mono` (`main` @ `6b4e141a`, 2026-08-17). Previous
sync: [2026-08-10](mono-sync-2026-08-10.md) (mono `ae1848f`, framework `01ec172` — unchanged by
this sync; nothing upstream touched the vendored surface). The gap covered was 46 first-parent
commits: the mono's run-013/014/015 remediation arcs plus ADR-0026 and a two-day fix burst
(mono PRs #270–#281). Landed on `feat/phase8-decider-forward-port` as six commits, each
referencing its mono source.

## Ported

| Commit | What | Source |
| --- | --- | --- |
| `e0bb319` | One fallback rule for cart-line identity: new `src/app/shop/cart-line-display.ts` (+7 tests); CartDrawer's `SKU: <id>` mislabel, CartChangedBanner's `SKU <id>`, and both checkout pages' bare-UUID fallbacks all render through it. `Variant <id>`, labelled, only when the line predates the R1 snapshot | mono #252 Phase 5a (`5480e…`) |
| `90db666` | Journey-keyed idempotency (`${cartId}-${totalPrice}` — the cartId IS the journey correlationId here, R5) and a mock `processPayment` that genuinely dedupes instead of discarding `_idempotencyKey`. Three property tests replacing the literal-pinning style that let the mono's version survive | mono #241 (`f42c3bda`), R4 half |
| `926a323` | The web tier tells the truth about domain failures: `cart-actions-outcome.ts` (`domainErrorOf`/`domainMessageOf`, cycle-guarded; +9 tests); wrappers log `ok: false` + reason for a refusal returned as a value and rethrow carrying the domain's sentence; CartContext's three "Unable to connect" sites show what the domain said | mono #242 (`75b30296`) |
| `609ce64` | **ADR-0026 routes-on-blocks**, cart as pilot (the mono's other pilot, inventory transfer, has no counterpart here): `routes?:` on `CommandBlock`, domain-local `deriveRoutes` with the three load-time laws, both state tables derived. Generator to schemaVersion 3: per-command journeys (`emits`/`via`/`declared`), deriveRoutes mirroring, journey rendering with wildcard fall-through. Three graph ratchets + five law tests. **Proof: edge triples 129 = 129, byte-identical** (mono's: 185 = 185); a permanent equivalence pin holds the derived tables equal to the deleted literals | mono #253 (`91e08773`) |
| `0df0d91` | The diagram stops lying about `delivered.updateStatus` (bare literal → `{ ...updateStatusBlock, guard: guardDeliveredUpdateStatus, enrich }`; generator follows spreads; two ratchets; **one-line doc diff**, same proof as upstream). Fulfiller failures keep their reason: `failureReason` on `FulfillerStatusUpdate` → `statusOutcome` → `FulfillerOrderFailed.errorMessage` (+3 tests) | mono #270 (`f0648dc0`) + #281 (`dc133c2f`) |
| this commit | This ledger | — |

Tests: 723 → **755** (+32) across the six commits; every CI gate green per commit, every ported
test/ratchet fail-proved by reverting its source change.

## Divergences this sync CHANGES

- **`mono-sync-2026-08-10.md` §OMS listed the `delivered.updateStatus` bare literal as an
  intentional divergence.** Half overturned, knowingly: that entry predates the mono's discovery
  (its #270) that the pattern makes the generated diagram state a falsehood. The spread is now the
  shared convention; the **guard override stays** (`guardDeliveredUpdateStatus` — the demo's
  refundability guard remains a real divergence, now expressed as an override on the spread).
- **Stay sentinel, new divergence stated:** this graph keeps the raw `__self` in
  `state-graph.json` (the mono's parser substitutes SELF with the state name). Render-level only —
  `*stays*` in diagrams.md — and the journeys ratchet accepts the sentinel. Preserves this port's
  edge-triple invariant.

## Verified already present (no change needed)

- §7.7 `reviewedCartVersion`/`CART_CHANGED` submit guard and live-cart re-pull (mono #229+#230) —
  the demo's own R3 covered it.
- Checkout pages read state from the CHECKOUT workflow, cart mirror as fallback only (mono #239).
- The admin id-sweep's `{ ids: { values } }` clause (the mono #270 `docId` fix's nearest analogue)
  has been present all along; there is no dead-letter index here.
- `docs:diagrams:check` able to fail (mono #228) — demo did its own first (`5744253`).

## Not ported, with reasons

| Mono change | Why not |
| --- | --- |
| #237 `paymentAlreadySucceeded` / `{kind:'paid'}` guard; #273/#276 §7.9a `paidAmount` money guard | **N/A without Stripe.** The demo captures payment only inside `submitOrder` — no pre-submit capture exists, so "already paid but not yet ordered" is unrepresentable and the mono's deadlock class cannot arise. WATCH: both become mandatory the day the demo gains real payments |
| #267/#268/#279 polling fixes (`getOrderStatus`, poll-failure classification, fail lever) | No polling strategy, no plugin system (`FulfillerAdapter.getOrderStatus` is interface-only here) |
| #269's `errorCode: FULFILLER_SUBMISSION_FAILED` half | The demo has no `FulfillmentErrorCode` taxonomy (unported mono -013 R10 work). The reason (`errorMessage`) is ported; the classification enum waits on that taxonomy — recorded, not silently skipped |
| #272/#277 shopper test-login | No admin auth or test-login route; demo shopper auth is its own `api/auth/shopper/*` |
| #235/#237 Stripe-intent reuse, #241 R8 Stripe retry mapping, #245–#251 reconciliation/dashboards, #252 Ph2/3/6/7, #260/#270 ops tools (park-hold, stamp-baseline, probe-unmatched-capture) | No Stripe, no Twisp ledger, no Grafana/Prometheus, no accounting domain |
| #243/#224 store-admin changes, #223 storeId derivation | No admin auth; single store. Lesson carried as a note: never parse a workflow id by string split — use the parser |
| #244 dynamic-fulfiller reachability + projection dashboard | Plugins + Grafana. Determinism note carried: env is read in the OMS *activity*, never in a workflow |
| mono #282 (seed per-fulfiller stock) | HELD — unfinished upstream (uncommitted). The demo's `seed-inventory/route.ts` hardcodes `default-fulfiller` but is single-fulfiller; watch item |
| ADR-0027, validation records/ledgers, `.env.example` simulated-dynamic knobs, git-strategy doc, skills-backup commit | Mono-specific records, config, and local agent tooling |

## Parked partials (portable later, deliberately not in this pass)

Mono #238 headless probes (back-nav, price-integrity), #234 link-builder delta vs the demo's own
R9, #233's journey-key-as-parameter lesson, #252 Phase 4's "checks that can actually fail"
pattern, `CheckoutCartMissing`, Phase 8's `edge-log` route. Mono #207 (derive display status at
read boundaries) remains deferred from the 08-09 ledger.
