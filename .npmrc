# The @temporalio/interceptors-opentelemetry package lags behind the main
# @temporalio/* releases, requiring legacy-peer-deps to resolve the mismatch
# between @temporalio/common@^1.17.0 and the interceptor's peer dep on 1.16.x.
legacy-peer-deps=true
